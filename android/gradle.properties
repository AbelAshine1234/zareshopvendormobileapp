org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Enable Gradle build cache to reuse outputs between builds
org.gradle.caching=true

# Enable parallel project execution
org.gradle.parallel=true

# Disable configuration cache due to current plugin/toolchain incompatibility
# Re-enable later when upgrading AGP/Kotlin/Gradle
org.gradle.configuration-cache=false

# Ensure Kotlin incremental compilation stays enabled
kotlin.incremental=true
